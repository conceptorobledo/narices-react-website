import React from 'react';
import Hero from './Hero';

import './Header.css';

const header = () => {
    return (
        <header>
            <Hero />
        </header>
    );
};

export default header;